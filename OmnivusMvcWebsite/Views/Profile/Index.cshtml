@model ProfileViewModel
@{
    ViewData["Title"] = $"Profile - {@Model.UserProfile.FirstName} {@Model.UserProfile.LastName}";
    Layout = "~/Views/Shared/_profileLayout.cshtml";
}

<div class="profileBackground">
    <div id="homeBtn">
        <a asp-controller="Home" asp-action="Index"><button>V</button></a>
    </div>
    <div id="profile" class="container">
        <div class="profileCard cardGrid">
            <div class="profilePlaceholder container">
                @if(@Model.UserProfile.FileName != null)
                {
                    <img src="@("~/img/"+@Model.UserProfile.FileName)" asp-append-version="true"
                     alt="placehodler.svg" />
                }
                else
                {
                    <img src="/img/profile_placeholder.svg"
                     alt="placehodler.svg" />
                }
                
            </div>
            <div class="profileName">
                <span>@Model.UserProfile.FirstName @Model.UserProfile.LastName</span>
            </div>
            <div class="profileRole">
                <span>@await ProfileManager.ReadRoleAsync(@Model.UserEntity.UserId)</span>
            </div>
            <div class="profileStreetName">
                <span>Street Name</span>
                <p class="mt-3">@Model.UserProfile.StreetName</p>
            </div>
            <div class="profilePostalCode">
                <span>Postal Code</span>
                <p class="mt-3">@Model.UserProfile.PostalCode</p>
            </div>
            <div class="profileCity">
                <span>City</span>
                <p class="mt-3">@Model.UserProfile.City</p>
            </div>
            <div class="profileCountry">
                <span>Country</span>
                <p class="mt-3">@Model.UserProfile.Country</p>
            </div>
            <div class="profileEmail">
                <span>Emailaddress</span>
                <p class="mt-3">@Model.UserProfile.Email</p>
            </div>
        </div>
        <div class="profileInfo">
            <div class="cardGrid container">
                <h1>About me</h1>
                <div class="bio container">
                    <textarea placeholder="Write about yourself"
                              class="questionField"
                              disabled>@Model.UserProfile.Bio
</textarea>
                </div>
                <div class="contentBtns"><a asp-controller="Profile" asp-action="Edit" asp-route-id="@Model.UserEntity.UserId"><button class="learnBtn">Edit profile</button></a></div>
            </div>
        </div>
    </div>
</div>